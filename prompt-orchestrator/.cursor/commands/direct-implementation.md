# Direct Implementation

This is the `direct-implementation` workflow from the prompt-orchestrator framework (canonical source: `workflows/direct-implementation.md`, also available at `.agents/skills/direct-implementation/SKILL.md`).

1. Load and follow the full instructions in that file exactly.
2. Treat everything after this line as the argument/context for the workflow, if anything was provided:

$ARGUMENTS
