# Browser Automation

This is the `browser-automation` skill from the prompt-orchestrator framework (canonical source: `skills/browser-automation.md`, also available at `.agents/skills/browser-automation/SKILL.md`).

1. Load and follow the full instructions in that file exactly.
2. Treat everything after this line as the argument/context for the skill, if anything was provided:

$ARGUMENTS
