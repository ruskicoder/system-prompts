---
trigger: always_on
description: Central AI Agent Orchestrator for this repo
---

Load and follow `AGENTS.md` at the repository root as your primary operating specification: identity & operating principles, the universal pre-action protocol, power modes, workflow routing, and the safety engine.

Reusable capabilities are available as Windsurf workflows under `.windsurf/workflows/` — type `/` in Cascade to browse and run them (for example `/agent-loop`, `/plan-execute`).
