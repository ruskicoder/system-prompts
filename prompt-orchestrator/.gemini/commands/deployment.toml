description = "Deploy applications, configure cloud hosting targets (Vercel, Netlify, GitHub Pages, S3), manage build pipelines, and verify post-deployment health. Trigger with \"deploy app\", \"publish release\"..."
prompt = "This is the 'deployment' skill from the prompt-orchestrator framework. Read and follow the instructions in '.agents/skills/deployment/SKILL.md' (canonical source: 'skills/deployment.md') exactly, then apply them to: {{args}}"
