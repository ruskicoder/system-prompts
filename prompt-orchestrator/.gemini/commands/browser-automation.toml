description = "Navigate, interact with, and extract data from web pages programmatically. Trigger with \"browse web page\", \"automate browser\", \"extract from URL\", or when performing web scraping, form filling, DOM..."
prompt = "This is the 'browser-automation' skill from the prompt-orchestrator framework. Read and follow the instructions in '.agents/skills/browser-automation/SKILL.md' (canonical source: 'skills/browser-automation.md') exactly, then apply them to: {{args}}"
